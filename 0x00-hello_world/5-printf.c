#include <stdio.h>
/**
 * main - a program that prints hello with printf function
 * return: 0(for success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
