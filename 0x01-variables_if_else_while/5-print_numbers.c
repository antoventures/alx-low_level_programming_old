#include <stdio.h>
/**
 * main - printing  Numbers
 * Return: 0 for success
 */
int main(void)
{
int count;
while (count = 0 && count < 10)
{
	putchar(count);
	count++;
}
putchar('\n');
return (0);
}
