#include <stdio.h>
/**
 * main - combinations of single-digit numbers.
 * Return: 0 success
 */

int main(void)
{
int i, s;
i = 0;
s = "\n";
while (i < 9)
{
putchar(i);
i++;
putchar(s);
}
return (0);
}
